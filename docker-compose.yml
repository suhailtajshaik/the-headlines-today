services:
  headlines:
    build: .
    container_name: the-headlines-today
    restart: unless-stopped
    ports:
      - "3006:80"
    volumes:
      - /home/r2d2/newspapers:/archive:ro
    networks:
      - webnet

networks:
  webnet:
    external: true
